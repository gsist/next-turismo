import SistemaLayoutAdm from "@/app/layouts/SistemaLayoutAdm";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <SistemaLayoutAdm >{children}</SistemaLayoutAdm>;
}
